Localiz - starter mobile-first (React JSX client + Node/Express server + MongoDB)

Structure:
- client/ (React + Vite)
- server/ (Express + Mongoose)

See the `client` and `server` folders for installation and run instructions.
